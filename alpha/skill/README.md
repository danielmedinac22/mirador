# Mirador skill

This directory is the Mirador Claude Code skill bundle. After running `mirador init`, the contents are copied to `~/.claude/skills/mirador/`.

The actual share-flow algorithm lives in `SKILL.md`.
